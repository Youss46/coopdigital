openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: CoopDigital — API de gestion des coopératives cacaoyères
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
  - name: auth
  - name: membres
  - name: avances
  - name: livraisons
  - name: dashboard
  - name: lots
  - name: stocks
  - name: exportateurs
  - name: communication
  - name: comptabilite
  - name: etats-financiers
  - name: rapports
  - name: users
  - name: salaires

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Connexion utilisateur
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginInput"
      responses:
        "200":
          description: Connexion réussie
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Identifiants invalides
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /membres:
    get:
      operationId: getMembres
      tags: [membres]
      summary: Liste paginée des membres
      parameters:
        - name: page
          in: query
          schema: { type: integer, default: 1 }
        - name: limit
          in: query
          schema: { type: integer, default: 20 }
        - name: search
          in: query
          schema: { type: string }
        - name: statut
          in: query
          schema: { type: string, enum: [actif, inactif] }
      responses:
        "200":
          description: Liste des membres
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MembrePagination"
    post:
      operationId: createMembre
      tags: [membres]
      summary: Créer un membre
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MembreInput"
      responses:
        "201":
          description: Membre créé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Membre"
        "400":
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /membres/qr/{token}:
    get:
      operationId: getMembreByQr
      tags: [membres]
      summary: Lookup membre par QR code
      parameters:
        - name: token
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Membre trouvé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Membre"
        "404":
          description: Non trouvé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /membres/{id}:
    get:
      operationId: getMembreById
      tags: [membres]
      summary: Fiche complète d'un membre
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Membre trouvé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Membre"
        "404":
          description: Non trouvé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      operationId: updateMembre
      tags: [membres]
      summary: Modifier un membre
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MembreUpdate"
      responses:
        "200":
          description: Membre modifié
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Membre"

  /membres/{id}/historique:
    get:
      operationId: getMembreHistorique
      tags: [membres]
      summary: Historique livraisons, avances et paiements d'un membre
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Historique du membre
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MembreHistorique"

  /avances:
    get:
      operationId: getAvances
      tags: [avances]
      summary: Liste des avances
      parameters:
        - name: statut
          in: query
          schema: { type: string, enum: [en_cours, rembourse, en_retard] }
        - name: membre_id
          in: query
          schema: { type: integer }
      responses:
        "200":
          description: Liste des avances
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvancePagination"
    post:
      operationId: createAvance
      tags: [avances]
      summary: Octroyer une avance
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AvanceInput"
      responses:
        "201":
          description: Avance octroyée
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Avance"
        "400":
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /avances/encours:
    get:
      operationId: getAvancesEncours
      tags: [avances]
      summary: Tableau de bord avances en cours
      responses:
        "200":
          description: Résumé avances en cours
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvancesEncours"

  /avances/{id}/rembourser:
    put:
      operationId: rembourserAvance
      tags: [avances]
      summary: Remboursement partiel ou total d'une avance
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RemboursementInput"
      responses:
        "200":
          description: Avance mise à jour
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Avance"
        "404":
          description: Non trouvée
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /livraisons:
    get:
      operationId: getLivraisons
      tags: [livraisons]
      summary: Liste des livraisons
      parameters:
        - name: membre_id
          in: query
          schema: { type: integer }
        - name: limit
          in: query
          schema: { type: integer, default: 20 }
      responses:
        "200":
          description: Liste des livraisons
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LivraisonDetail"
    post:
      operationId: createLivraison
      tags: [livraisons]
      summary: Enregistrer une livraison avec déduction d'avance automatique
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LivraisonInput"
      responses:
        "201":
          description: Livraison enregistrée
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LivraisonResult"
        "400":
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /livraisons/non-lotees:
    get:
      operationId: getLivraisonsNonLotees
      tags: [livraisons]
      summary: Livraisons non encore affectées à un lot
      responses:
        "200":
          description: Livraisons disponibles pour création de lot
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LivraisonDetail"

  /dashboard:
    get:
      operationId: getDashboard
      tags: [dashboard]
      summary: KPIs du tableau de bord
      responses:
        "200":
          description: Indicateurs clés
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardKpi"

  /dashboard/livraisons:
    get:
      operationId: getDashboardLivraisons
      tags: [dashboard]
      summary: Dernières livraisons (5)
      responses:
        "200":
          description: Dernières livraisons
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LivraisonDetail"

  /dashboard/avances-retard:
    get:
      operationId: getDashboardAvancesRetard
      tags: [dashboard]
      summary: Avances en retard
      responses:
        "200":
          description: Avances en retard
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Avance"

  # ── M02 Traçabilité ────────────────────────────────────────────
  /lots:
    get:
      operationId: getLots
      tags: [lots]
      summary: Liste des lots
      parameters:
        - name: statut
          in: query
          schema: { type: string, enum: [en_stock, vendu, transit] }
      responses:
        "200":
          description: Liste des lots
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LotDetail"
    post:
      operationId: createLot
      tags: [lots]
      summary: Créer un lot à partir de livraisons
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LotInput"
      responses:
        "201":
          description: Lot créé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LotDetail"
        "400":
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /lots/qr/{code}:
    get:
      operationId: getLotByQr
      tags: [lots]
      summary: Lookup lot par QR code
      parameters:
        - name: code
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Lot trouvé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LotDetail"
        "404":
          description: Non trouvé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /lots/{id}/statut:
    put:
      operationId: updateLotStatut
      tags: [lots]
      summary: Changer le statut d'un lot
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LotStatutInput"
      responses:
        "200":
          description: Statut mis à jour
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LotDetail"
        "404":
          description: Non trouvé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /lots/{id}/tracabilite:
    get:
      operationId: getLotTracabilite
      tags: [lots]
      summary: Chaîne complète de traçabilité d'un lot
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Chaîne de traçabilité
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LotTracabilite"
        "404":
          description: Non trouvé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  # ── M03 Stocks ────────────────────────────────────────────────
  /stocks/entrepots:
    get:
      operationId: getEntrepots
      tags: [stocks]
      summary: Liste des entrepôts avec stock actuel calculé
      responses:
        "200":
          description: Entrepôts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EntrepotStock"

  /stocks/mouvements:
    get:
      operationId: getMouvementsStock
      tags: [stocks]
      summary: Journal des mouvements de stock
      parameters:
        - name: entrepot_id
          in: query
          schema: { type: integer }
        - name: date_debut
          in: query
          schema: { type: string }
        - name: date_fin
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Mouvements
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MouvementStock"

  /stocks/entree:
    post:
      operationId: entreeStock
      tags: [stocks]
      summary: Entrée manuelle en stock
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MouvementInput"
      responses:
        "201":
          description: Mouvement enregistré
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MouvementStock"
        "400":
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /stocks/sortie:
    post:
      operationId: sortieStock
      tags: [stocks]
      summary: Sortie de stock vers exportateur
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MouvementInput"
      responses:
        "201":
          description: Mouvement enregistré
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MouvementStock"
        "400":
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /stocks/alertes:
    get:
      operationId: getStockAlertes
      tags: [stocks]
      summary: Entrepôts sous seuil minimum
      responses:
        "200":
          description: Alertes stock
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EntrepotStock"

  # ── M04 étendu Exportateurs ───────────────────────────────────
  /exportateurs:
    get:
      operationId: getExportateurs
      tags: [exportateurs]
      summary: Liste des exportateurs avec solde créances
      responses:
        "200":
          description: Exportateurs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ExportateurDetail"
    post:
      operationId: createExportateur
      tags: [exportateurs]
      summary: Créer un exportateur
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExportateurInput"
      responses:
        "201":
          description: Exportateur créé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExportateurDetail"
        "400":
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /exportateurs/{id}:
    get:
      operationId: getExportateurById
      tags: [exportateurs]
      summary: Détail exportateur avec historique ventes
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Exportateur trouvé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExportateurHistorique"
        "404":
          description: Non trouvé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /ventes:
    get:
      operationId: getVentes
      tags: [exportateurs]
      summary: Liste des ventes
      parameters:
        - name: exportateur_id
          in: query
          schema: { type: integer }
        - name: statut
          in: query
          schema: { type: string, enum: [en_attente, partiel, regle, en_retard] }
      responses:
        "200":
          description: Ventes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/VenteDetail"
    post:
      operationId: createVente
      tags: [exportateurs]
      summary: Enregistrer une vente à crédit
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VenteInput"
      responses:
        "201":
          description: Vente enregistrée
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VenteDetail"
        "400":
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /ventes/creances:
    get:
      operationId: getCreances
      tags: [exportateurs]
      summary: Tableau des créances (dû, en retard, à échoir)
      responses:
        "200":
          description: Créances
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreancesSummary"

  /ventes/{id}/encaissement:
    put:
      operationId: encaisserVente
      tags: [exportateurs]
      summary: Enregistrer un paiement reçu sur une vente
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EncaissementInput"
      responses:
        "200":
          description: Vente mise à jour
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VenteDetail"
        "404":
          description: Non trouvée
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  # ── M07 Communication ─────────────────────────────────────────
  /communication/sms-groupe:
    post:
      operationId: sendSmsGroupe
      tags: [communication]
      summary: Envoyer un SMS groupé aux membres
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SmsGroupeInput"
      responses:
        "200":
          description: SMS envoyé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SmsGroupeResult"
        "400":
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /communication/historique:
    get:
      operationId: getCommunicationHistorique
      tags: [communication]
      summary: Historique des envois SMS
      responses:
        "200":
          description: Historique
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SmsHistorique"

  # ── M05 Comptabilité ─────────────────────────────────────────
  /comptabilite/grand-livre:
    get:
      operationId: getGrandLivre
      tags: [comptabilite]
      summary: Grand-livre – toutes les écritures
      parameters:
        - name: compte
          in: query
          schema: { type: string }
        - name: date_debut
          in: query
          schema: { type: string }
        - name: date_fin
          in: query
          schema: { type: string }
        - name: exercice
          in: query
          schema: { type: integer }
        - name: page
          in: query
          schema: { type: integer, default: 1 }
        - name: limit
          in: query
          schema: { type: integer, default: 50 }
      responses:
        "200":
          description: Liste paginée des écritures
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrandLivrePage"
        "401":
          description: Non authentifié

  /comptabilite/balance:
    get:
      operationId: getBalance
      tags: [comptabilite]
      summary: Balance des comptes (débit / crédit / solde)
      parameters:
        - name: exercice
          in: query
          schema: { type: integer }
      responses:
        "200":
          description: Balance par compte
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BalanceLigne"
        "401":
          description: Non authentifié

  /comptabilite/journal:
    get:
      operationId: getJournalComptable
      tags: [comptabilite]
      summary: Journal comptable chronologique
      parameters:
        - name: exercice
          in: query
          schema: { type: integer }
        - name: page
          in: query
          schema: { type: integer, default: 1 }
        - name: limit
          in: query
          schema: { type: integer, default: 50 }
      responses:
        "200":
          description: Journal paginé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GrandLivrePage"
        "401":
          description: Non authentifié

  /comptabilite/ecriture:
    post:
      operationId: createEcritureManuelle
      tags: [comptabilite]
      summary: Écriture manuelle (admin uniquement)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EcritureManuelleInput"
      responses:
        "201":
          description: Écriture créée
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EcritureComptable"
        "401":
          description: Non authentifié

  /comptabilite/marge-collecte:
    get:
      operationId: getMargeCollecte
      tags: [comptabilite]
      summary: Marge de collecte (CA – achats – charges)
      parameters:
        - name: exercice
          in: query
          schema: { type: integer }
      responses:
        "200":
          description: Marge de collecte
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MargeCollecte"
        "401":
          description: Non authentifié

  /comptabilite/tresorerie:
    get:
      operationId: getTresorerie
      tags: [comptabilite]
      summary: Soldes Banque + Caisse en temps réel
      responses:
        "200":
          description: Situation de trésorerie
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tresorerie"
        "401":
          description: Non authentifié

  # ── Config comptable & écritures en attente ──────────────────
  /comptabilite/config:
    get:
      operationId: getConfigComptable
      tags: [comptabilite]
      summary: Récupère la configuration comptable de la coopérative
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Configuration comptable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigComptable"
        "401":
          description: Non authentifié
    put:
      operationId: updateConfigComptable
      tags: [comptabilite]
      summary: Met à jour la configuration comptable
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateConfigComptableInput"
      responses:
        "200":
          description: Configuration mise à jour
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigComptable"
        "401":
          description: Non authentifié

  /comptabilite/en-attente/count:
    get:
      operationId: countEcrituresEnAttente
      tags: [comptabilite]
      summary: Nombre d'écritures en attente de validation
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Compteur
          content:
            application/json:
              schema:
                type: object
                required: [count]
                properties:
                  count:
                    type: integer
        "401":
          description: Non authentifié

  /comptabilite/en-attente:
    get:
      operationId: listEcrituresEnAttente
      tags: [comptabilite]
      summary: Liste les écritures en attente (et historique)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: source
          in: query
          schema:
            type: string
            enum: [livraison, paiement, avance, vente, encaissement, salaire, stock]
        - name: statut
          in: query
          schema:
            type: string
            enum: [en_attente, validee, rejetee, modifiee]
        - name: date_debut
          in: query
          schema:
            type: string
            format: date
        - name: date_fin
          in: query
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Liste des écritures en attente
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EcritureEnAttente"
        "401":
          description: Non authentifié

  /comptabilite/en-attente/valider-tout:
    put:
      operationId: validerToutEcrituresEnAttente
      tags: [comptabilite]
      summary: Valide toutes les écritures en attente
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Nombre d'écritures validées
          content:
            application/json:
              schema:
                type: object
                required: [validees]
                properties:
                  validees:
                    type: integer
        "401":
          description: Non authentifié

  /comptabilite/en-attente/{id}/valider:
    put:
      operationId: validerEcritureEnAttente
      tags: [comptabilite]
      summary: Valide (et éventuellement modifie) une écriture en attente
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ValiderEcritureInput"
      responses:
        "200":
          description: Écriture traitée
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EcritureEnAttente"
        "400":
          description: Écriture déjà traitée
        "404":
          description: Écriture introuvable
        "401":
          description: Non authentifié

  /comptabilite/en-attente/{id}/rejeter:
    put:
      operationId: rejeterEcritureEnAttente
      tags: [comptabilite]
      summary: Rejette une écriture en attente
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejeterEcritureInput"
      responses:
        "200":
          description: Écriture rejetée
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EcritureEnAttente"
        "400":
          description: Motif obligatoire ou écriture déjà traitée
        "404":
          description: Écriture introuvable
        "401":
          description: Non authentifié

  # ── M05 États financiers ──────────────────────────────────────
  /etats-financiers/bilan:
    get:
      operationId: getBilan
      tags: [etats-financiers]
      summary: Bilan OHADA (actif / passif)
      parameters:
        - name: exercice
          in: query
          schema: { type: integer }
      responses:
        "200":
          description: Bilan structuré
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BilanEtat"
        "401":
          description: Non authentifié

  /etats-financiers/compte-resultat:
    get:
      operationId: getCompteResultat
      tags: [etats-financiers]
      summary: Compte de résultat OHADA
      parameters:
        - name: exercice
          in: query
          schema: { type: integer }
      responses:
        "200":
          description: Compte de résultat
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompteResultat"
        "401":
          description: Non authentifié

  /etats-financiers/flux-tresorerie:
    get:
      operationId: getFluxTresorerie
      tags: [etats-financiers]
      summary: Flux de trésorerie
      parameters:
        - name: exercice
          in: query
          schema: { type: integer }
      responses:
        "200":
          description: Flux de trésorerie
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FluxTresorerie"
        "401":
          description: Non authentifié

  /etats-financiers/marge-par-campagne:
    get:
      operationId: getMargeCampagnes
      tags: [etats-financiers]
      summary: Marge nette par campagne (comparatif)
      responses:
        "200":
          description: Marges par campagne
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MargeCampagne"
        "401":
          description: Non authentifié

  /users:
    get:
      operationId: getUsers
      tags: [users]
      summary: Liste tous les comptes utilisateurs
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Liste des utilisateurs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UtilisateurCompte"
        "403":
          description: Accès refusé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      operationId: createUser
      tags: [users]
      summary: Créer un compte utilisateur
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateUserInput"
      responses:
        "201":
          description: Compte créé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UtilisateurCompte"
        "400":
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Accès refusé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /users/{id}:
    put:
      operationId: updateUser
      tags: [users]
      summary: Modifier les informations d'un compte
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserInput"
      responses:
        "200":
          description: Compte mis à jour
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UtilisateurCompte"
        "404":
          description: Compte introuvable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteUser
      tags: [users]
      summary: Supprimer un compte utilisateur
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204":
          description: Compte supprimé
        "403":
          description: Action non autorisée
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /users/{id}/password:
    put:
      operationId: resetUserPassword
      tags: [users]
      summary: Réinitialiser le mot de passe d'un compte
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResetPasswordInput"
      responses:
        "200":
          description: Mot de passe réinitialisé
          content:
            application/json:
              schema:
                type: object
                properties:
                  message: { type: string }
        "403":
          description: Accès refusé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /users/{id}/activer:
    put:
      operationId: toggleUserActif
      tags: [users]
      summary: Activer ou désactiver un compte
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToggleActifInput"
      responses:
        "200":
          description: Statut mis à jour
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UtilisateurCompte"
        "403":
          description: Accès refusé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /salaires/personnel:
    get:
      operationId: getPersonnel
      tags: [salaires]
      summary: Liste du personnel
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Liste du personnel
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Personnel"
    post:
      operationId: createPersonnel
      tags: [salaires]
      summary: Créer un membre du personnel
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePersonnelInput"
      responses:
        "201":
          description: Personnel créé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Personnel"
        "400":
          description: Données invalides
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /salaires/personnel/{id}:
    get:
      operationId: getPersonnelById
      tags: [salaires]
      summary: Détail d'un membre du personnel
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Personnel
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Personnel"
        "404":
          description: Introuvable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      operationId: updatePersonnel
      tags: [salaires]
      summary: Modifier un membre du personnel
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePersonnelInput"
      responses:
        "200":
          description: Personnel mis à jour
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Personnel"
    delete:
      operationId: archiverPersonnel
      tags: [salaires]
      summary: Archiver un membre du personnel (statut = sorti)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Personnel archivé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Personnel"

  /salaires/personnel/{id}/historique:
    get:
      operationId: getPersonnelHistorique
      tags: [salaires]
      summary: Historique bulletins + avances d'un membre du personnel
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Historique
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonnelHistorique"

  /salaires/composantes:
    get:
      operationId: getComposantesSalaire
      tags: [salaires]
      summary: Catalogue des composantes de salaire
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Composantes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ComposanteSalaire"

  /salaires/bulletins/generer:
    post:
      operationId: genererBulletins
      tags: [salaires]
      summary: Générer les bulletins de paie (masse ou sélection)
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenererBulletinsInput"
      responses:
        "200":
          description: Résultats de génération
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GenerationResult"

  /salaires/bulletins:
    get:
      operationId: getBulletins
      tags: [salaires]
      summary: Liste des bulletins de paie
      security: [{ bearerAuth: [] }]
      parameters:
        - name: mois
          in: query
          schema: { type: integer }
        - name: annee
          in: query
          schema: { type: integer }
        - name: statut
          in: query
          schema:
            type: string
            enum: [brouillon, valide, paye]
      responses:
        "200":
          description: Liste des bulletins
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BulletinAvecPersonnel"

  /salaires/bulletins/{id}:
    get:
      operationId: getBulletinById
      tags: [salaires]
      summary: Détail d'un bulletin de paie avec lignes
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Bulletin détaillé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulletinDetail"
        "404":
          description: Introuvable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteBulletin
      tags: [salaires]
      summary: Supprimer un bulletin (brouillon uniquement)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204":
          description: Supprimé

  /salaires/bulletins/{id}/valider:
    put:
      operationId: validerBulletin
      tags: [salaires]
      summary: Valider un bulletin de paie
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Bulletin validé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulletinPaie"

  /salaires/bulletins/{id}/payer:
    put:
      operationId: payerBulletin
      tags: [salaires]
      summary: Marquer un bulletin comme payé
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PayerBulletinInput"
      responses:
        "200":
          description: Bulletin payé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulletinPaie"

  /salaires/bulletins/{id}/pdf:
    get:
      operationId: getBulletinPdf
      tags: [salaires]
      summary: Télécharger le bulletin en PDF
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: PDF du bulletin
          content:
            application/pdf:
              schema:
                type: string
                format: binary

  /salaires/avances:
    get:
      operationId: getAvancesPersonnel
      tags: [salaires]
      summary: Liste des avances personnel
      security: [{ bearerAuth: [] }]
      parameters:
        - name: personnelId
          in: query
          schema: { type: integer }
        - name: statut
          in: query
          schema:
            type: string
            enum: [en_cours, rembourse]
      responses:
        "200":
          description: Liste des avances
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AvanceAvecPersonnel"
    post:
      operationId: createAvancePersonnel
      tags: [salaires]
      summary: Octroyer une avance à un membre du personnel
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAvancePersonnelInput"
      responses:
        "201":
          description: Avance créée
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvancePersonnel"

  /salaires/avances/{id}/rembourser:
    put:
      operationId: rembourserAvancePersonnel
      tags: [salaires]
      summary: Enregistrer un remboursement d'avance
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RemboursementAvancePersonnelInput"
      responses:
        "200":
          description: Avance mise à jour
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvancePersonnel"

  /salaires/rapport-mensuel/{mois}/{annee}:
    get:
      operationId: getRapportMensuel
      tags: [salaires]
      summary: Rapport de masse salariale mensuel
      security: [{ bearerAuth: [] }]
      parameters:
        - name: mois
          in: path
          required: true
          schema: { type: integer }
        - name: annee
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Rapport mensuel
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RapportMensuel"

  /salaires/historique-masse:
    get:
      operationId: getHistoriqueMasse
      tags: [salaires]
      summary: Évolution de la masse salariale sur 12 mois
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Historique masse salariale
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HistoriqueMasse"

  /api/campagnes/active:
    get:
      operationId: getCampagneActive
      tags: [campagnes]
      summary: Campagne en cours (statut ouverte)
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Campagne active
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Campagne"
        "404":
          description: Aucune campagne active

  /api/campagnes:
    get:
      operationId: listCampagnes
      tags: [campagnes]
      summary: Liste de toutes les campagnes
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Liste campagnes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Campagne"
    post:
      operationId: createCampagne
      tags: [campagnes]
      summary: Créer une nouvelle campagne
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CampagneInput"
      responses:
        "201":
          description: Campagne créée
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Campagne"

  /api/campagnes/{id}/fermer:
    put:
      operationId: fermerCampagne
      tags: [campagnes]
      summary: Clôturer une campagne
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FermerCampagneInput"
      responses:
        "200":
          description: Campagne fermée
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Campagne"

  /api/parts-sociales/config:
    get:
      operationId: getConfigParts
      tags: [parts-sociales]
      summary: Config parts sociales de la coopérative
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigPartsSociales"
    put:
      operationId: updateConfigParts
      tags: [parts-sociales]
      summary: Modifier la config parts sociales
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfigPartsSociales"
      responses:
        "200":
          description: Config mise à jour
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigPartsSociales"

  /api/parts-sociales/rapport:
    get:
      operationId: getRapportParts
      tags: [parts-sociales]
      summary: Rapport global parts sociales
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Rapport
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RapportParts"

  /api/parts-sociales/membre/{id}:
    get:
      operationId: getPartsMembre
      tags: [parts-sociales]
      summary: Parts sociales d'un membre
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Parts du membre
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartsMembre"

  /api/parts-sociales/liberation:
    post:
      operationId: enregistrerLiberation
      tags: [parts-sociales]
      summary: Enregistrer un versement de libération
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LiberationInput"
      responses:
        "201":
          description: Versement enregistré
          content:
            application/json:
              schema:
                type: object
                required: [succes]
                properties:
                  succes:
                    type: boolean

  /api/refus/count:
    get:
      operationId: countRefusEnAttente
      tags: [refus]
      summary: Nombre de refus en attente de traitement
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Comptage
          content:
            application/json:
              schema:
                type: object
                required: [count]
                properties:
                  count:
                    type: integer

  /api/refus/stats:
    get:
      operationId: getStatsRefus
      tags: [refus]
      summary: Statistiques stocks refoulés
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RefusStats"

  /api/refus:
    get:
      operationId: listRefus
      tags: [refus]
      summary: Liste des stocks refoulés
      security: [{ bearerAuth: [] }]
      parameters:
        - name: statut
          in: query
          schema:
            type: string
            enum: [en_attente, traite]
      responses:
        "200":
          description: Liste refus
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Refus"

  /api/refus/{id}/traiter:
    put:
      operationId: traiterRefus
      tags: [refus]
      summary: Traiter un lot refoulé (décision)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TraiterRefusInput"
      responses:
        "200":
          description: Refus traité
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Refus"

  /api/fournisseurs/search:
    get:
      operationId: searchFournisseurs
      tags: [fournisseurs]
      summary: Recherche autocomplete fournisseurs
      security: [{ bearerAuth: [] }]
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Résultats
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Fournisseur"

  /api/fournisseurs/rapport-type:
    get:
      operationId: getRapportTypeFournisseur
      tags: [fournisseurs]
      summary: Répartition des fournisseurs par type
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Rapport
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required: [typeFournisseur, count]
                  properties:
                    typeFournisseur:
                      type: string
                    count:
                      type: integer

  /api/fournisseurs:
    get:
      operationId: listFournisseurs
      tags: [fournisseurs]
      summary: Liste des fournisseurs
      security: [{ bearerAuth: [] }]
      parameters:
        - name: type
          in: query
          schema:
            type: string
            enum: [membre, pisteur, externe]
        - name: section
          in: query
          schema:
            type: string
        - name: q
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Liste fournisseurs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Fournisseur"
    post:
      operationId: createFournisseur
      tags: [fournisseurs]
      summary: Créer un fournisseur (pisteur/externe)
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FournisseurInput"
      responses:
        "201":
          description: Fournisseur créé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Fournisseur"

  /api/fournisseurs/{id}:
    get:
      operationId: getFournisseurById
      tags: [fournisseurs]
      summary: Fiche fournisseur
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Fournisseur
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Fournisseur"
    put:
      operationId: updateFournisseur
      tags: [fournisseurs]
      summary: Modifier un fournisseur
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FournisseurInput"
      responses:
        "200":
          description: Fournisseur mis à jour
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Fournisseur"

  /api/fournisseurs/depuis-membre/{id}:
    post:
      operationId: createFournisseurDepuisMembre
      tags: [fournisseurs]
      summary: Créer un fournisseur depuis un membre existant
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "201":
          description: Fournisseur créé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Fournisseur"

  /api/paiements:
    get:
      operationId: listPaiements
      tags: [paiements]
      summary: Liste des paiements producteurs
      security: [{ bearerAuth: [] }]
      parameters:
        - name: statut
          in: query
          schema:
            type: string
            enum: [en_attente, confirme, echec]
        - name: membre_id
          in: query
          schema:
            type: integer
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
      responses:
        "200":
          description: Liste paiements
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PaiementListItem"

  /api/paiements/{id}/valider:
    patch:
      operationId: validerPaiement
      tags: [paiements]
      summary: Marquer un paiement comme confirmé
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ValiderPaiementInput"
      responses:
        "200":
          description: Paiement confirmé
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaiementListItem"

components:
  schemas:
    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string

    ErrorResponse:
      type: object
      required: [erreur]
      properties:
        erreur:
          type: string

    LoginInput:
      type: object
      required: [email, motDePasse]
      properties:
        email:
          type: string
          format: email
        motDePasse:
          type: string

    AuthResponse:
      type: object
      required: [token, utilisateur]
      properties:
        token:
          type: string
        utilisateur:
          $ref: "#/components/schemas/UserProfile"

    UserProfile:
      type: object
      required: [id, nom, prenoms, role]
      properties:
        id:
          type: integer
        nom:
          type: string
        prenoms:
          type: string
        role:
          type: string
        cooperativeId:
          type: ["integer", "null"]

    Membre:
      type: object
      required: [id, cooperativeId, nom, prenoms, telephone, superficieHa, statut, qrCodeToken, dateAdhesion, createdAt, updatedAt]
      properties:
        id:
          type: integer
        cooperativeId:
          type: integer
        nom:
          type: string
        prenoms:
          type: string
        numeroCni:
          type: ["string", "null"]
        telephone:
          type: string
        village:
          type: ["string", "null"]
        groupement:
          type: ["string", "null"]
        superficieHa:
          type: string
        statut:
          type: string
          enum: [actif, inactif]
        qrCodeToken:
          type: string
        dateAdhesion:
          type: string
        photoUrl:
          type: ["string", "null"]
        parcelleLat:
          type: ["string", "null"]
        parcelleLng:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string

    MembreInput:
      type: object
      required: [cooperativeId, nom, prenoms, telephone, superficieHa, dateAdhesion]
      properties:
        cooperativeId:
          type: integer
        nom:
          type: string
        prenoms:
          type: string
        numeroCni:
          type: string
        telephone:
          type: string
        village:
          type: string
        groupement:
          type: string
        superficieHa:
          type: string
        statut:
          type: string
          enum: [actif, inactif]
        dateAdhesion:
          type: string
        photoUrl:
          type: string
        parcelleLat:
          type: string
        parcelleLng:
          type: string

    MembreUpdate:
      type: object
      properties:
        nom:
          type: string
        prenoms:
          type: string
        numeroCni:
          type: string
        telephone:
          type: string
        village:
          type: string
        groupement:
          type: string
        superficieHa:
          type: string
        statut:
          type: string
          enum: [actif, inactif]
        photoUrl:
          type: string
        parcelleLat:
          type: string
        parcelleLng:
          type: string

    MembrePagination:
      type: object
      required: [membres, total, page, limit]
      properties:
        membres:
          type: array
          items:
            $ref: "#/components/schemas/Membre"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer

    MembreHistorique:
      type: object
      required: [livraisons, avances, paiements]
      properties:
        livraisons:
          type: array
          items:
            $ref: "#/components/schemas/LivraisonDetail"
        avances:
          type: array
          items:
            $ref: "#/components/schemas/Avance"
        paiements:
          type: array
          items:
            $ref: "#/components/schemas/Paiement"

    Avance:
      type: object
      required: [id, membreId, montantOctroyeFcfa, montantRembourseFcfa, soldeRestantFcfa, dateOctroi, statut, createdAt]
      properties:
        id:
          type: integer
        membreId:
          type: integer
        montantOctroyeFcfa:
          type: integer
        montantRembourseFcfa:
          type: integer
        soldeRestantFcfa:
          type: integer
        dateOctroi:
          type: string
        dateEcheance:
          type: ["string", "null"]
        motif:
          type: ["string", "null"]
        statut:
          type: string
          enum: [en_cours, rembourse, en_retard]
        agentId:
          type: ["integer", "null"]
        createdAt:
          type: string
        membreNom:
          type: ["string", "null"]
        membrePrenoms:
          type: ["string", "null"]

    AvanceInput:
      type: object
      required: [membreId, montantOctroyeFcfa, dateOctroi]
      properties:
        membreId:
          type: integer
        montantOctroyeFcfa:
          type: integer
        dateOctroi:
          type: string
        dateEcheance:
          type: string
        motif:
          type: string

    RemboursementInput:
      type: object
      required: [montantFcfa]
      properties:
        montantFcfa:
          type: integer

    AvancePagination:
      type: object
      required: [avances, total]
      properties:
        avances:
          type: array
          items:
            $ref: "#/components/schemas/Avance"
        total:
          type: integer

    AvancesEncours:
      type: object
      required: [totalOctroye, totalRembourse, soldeToral, count, avances]
      properties:
        totalOctroye:
          type: integer
        totalRembourse:
          type: integer
        soldeToral:
          type: integer
        count:
          type: integer
        avances:
          type: array
          items:
            $ref: "#/components/schemas/Avance"

    LivraisonInput:
      type: object
      required: [membreId, poidsKg, prixUnitaireFcfa, dateLivraison]
      properties:
        membreId:
          type: integer
        poidsKg:
          type: number
        prixUnitaireFcfa:
          type: integer
        dateLivraison:
          type: string
        modePaiement:
          type: string
          enum: [orange_money, mtn_momo, especes]
        campagneId:
          type: integer
          nullable: true
        nombreSacs:
          type: integer
          nullable: true
        retenueKg:
          type: number
          nullable: true
        sectionLivraison:
          type: string
          nullable: true

    ValiderPaiementInput:
      type: object
      properties:
        referenceTransaction:
          type: string
          nullable: true
        dateReglement:
          type: string
          nullable: true

    PaiementListItem:
      type: object
      required: [id, livraisonId, membreId, montantFcfa, modePaiement, statut, createdAt]
      properties:
        id:
          type: integer
        livraisonId:
          type: integer
        membreId:
          type: integer
        montantFcfa:
          type: integer
        modePaiement:
          type: string
          enum: [orange_money, mtn_momo, especes]
        referenceTransaction:
          type: ["string", "null"]
        statut:
          type: string
          enum: [en_attente, confirme, echec]
        createdAt:
          type: string
        membreNom:
          type: ["string", "null"]
        membrePrenoms:
          type: ["string", "null"]
        telephone:
          type: ["string", "null"]
        dateLivraison:
          type: ["string", "null"]

    LivraisonDetail:
      type: object
      required: [id, membreId, poidsKg, prixUnitaireFcfa, montantBrutFcfa, avanceDeduiteFcfa, montantNetFcfa, dateLivraison, createdAt]
      properties:
        id:
          type: integer
        membreId:
          type: integer
        poidsKg:
          type: string
        prixUnitaireFcfa:
          type: integer
        montantBrutFcfa:
          type: integer
        avanceDeduiteFcfa:
          type: integer
        montantNetFcfa:
          type: integer
        dateLivraison:
          type: string
        agentId:
          type: ["integer", "null"]
        createdAt:
          type: string
        membreNom:
          type: ["string", "null"]
        membrePrenoms:
          type: ["string", "null"]

    LivraisonResult:
      type: object
      required: [livraison, paiement]
      properties:
        livraison:
          $ref: "#/components/schemas/LivraisonDetail"
        paiement:
          $ref: "#/components/schemas/Paiement"
        avanceMiseAJour:
          $ref: "#/components/schemas/Avance"

    Paiement:
      type: object
      required: [id, livraisonId, membreId, montantFcfa, modePaiement, statut, recuEnvoyeWhatsapp, createdAt]
      properties:
        id:
          type: integer
        livraisonId:
          type: integer
        membreId:
          type: integer
        montantFcfa:
          type: integer
        modePaiement:
          type: string
          enum: [orange_money, mtn_momo, especes]
        referenceTransaction:
          type: ["string", "null"]
        statut:
          type: string
          enum: [en_attente, confirme, echec]
        recuEnvoyeWhatsapp:
          type: boolean
        createdAt:
          type: string

    DashboardKpi:
      type: object
      required: [membresActifs, avancesEnCoursMontant, tonnageMois, paiementsMois]
      properties:
        membresActifs:
          type: integer
        avancesEnCoursMontant:
          type: integer
        tonnageMois:
          type: number
        paiementsMois:
          type: integer
        creancesExportateurs:
          type: integer

    # ── M02 Lots ────────────────────────────────────────────────
    LotDetail:
      type: object
      required: [id, cooperativeId, qrCodeLot, statut, poidsTotalKg, dateCreation, createdAt]
      properties:
        id:
          type: integer
        cooperativeId:
          type: integer
        qrCodeLot:
          type: string
        statut:
          type: string
          enum: [en_stock, vendu, transit]
        poidsTotalKg:
          type: string
        dateCreation:
          type: string
        entrepot:
          type: ["string", "null"]
        createdAt:
          type: string
        nbProducteurs:
          type: integer
        nbLivraisons:
          type: integer

    LotInput:
      type: object
      required: [cooperativeId, livraisonIds]
      properties:
        cooperativeId:
          type: integer
        livraisonIds:
          type: array
          items:
            type: integer
        entrepot:
          type: string

    LotStatutInput:
      type: object
      required: [statut]
      properties:
        statut:
          type: string
          enum: [en_stock, vendu, transit]

    LotTracabilite:
      type: object
      required: [lot, livraisons, membres]
      properties:
        lot:
          $ref: "#/components/schemas/LotDetail"
        livraisons:
          type: array
          items:
            $ref: "#/components/schemas/LivraisonDetail"
        membres:
          type: array
          items:
            $ref: "#/components/schemas/Membre"
        vente:
          $ref: "#/components/schemas/VenteDetail"

    # ── M03 Stocks ──────────────────────────────────────────────
    EntrepotStock:
      type: object
      required: [id, cooperativeId, nom, ville, capaciteKg, stockActuelKg, createdAt]
      properties:
        id:
          type: integer
        cooperativeId:
          type: integer
        nom:
          type: string
        ville:
          type: string
        capaciteKg:
          type: string
        seuilAlerteKg:
          type: ["string", "null"]
        stockActuelKg:
          type: number
        pourcentageRemplissage:
          type: number
        enAlerte:
          type: boolean
        createdAt:
          type: string

    MouvementStock:
      type: object
      required: [id, entrepotId, type, poidsKg, createdAt]
      properties:
        id:
          type: integer
        entrepotId:
          type: integer
        entrepotNom:
          type: ["string", "null"]
        lotId:
          type: ["integer", "null"]
        type:
          type: string
          enum: [entree, sortie]
        poidsKg:
          type: string
        motif:
          type: ["string", "null"]
        agentId:
          type: ["integer", "null"]
        createdAt:
          type: string

    MouvementInput:
      type: object
      required: [entrepotId, poidsKg]
      properties:
        entrepotId:
          type: integer
        lotId:
          type: integer
        poidsKg:
          type: number
        motif:
          type: string

    # ── M04 étendu Exportateurs ─────────────────────────────────
    ExportateurDetail:
      type: object
      required: [id, cooperativeId, nom, createdAt]
      properties:
        id:
          type: integer
        cooperativeId:
          type: integer
        nom:
          type: string
        contact:
          type: ["string", "null"]
        ville:
          type: ["string", "null"]
        agrementNumero:
          type: ["string", "null"]
        soldeTotalDuFcfa:
          type: integer
        createdAt:
          type: string

    ExportateurHistorique:
      type: object
      required: [exportateur, ventes, soldeTotalDuFcfa]
      properties:
        exportateur:
          $ref: "#/components/schemas/ExportateurDetail"
        ventes:
          type: array
          items:
            $ref: "#/components/schemas/VenteDetail"
        soldeTotalDuFcfa:
          type: integer

    ExportateurInput:
      type: object
      required: [cooperativeId, nom]
      properties:
        cooperativeId:
          type: integer
        nom:
          type: string
        contact:
          type: string
        ville:
          type: string
        agrementNumero:
          type: string

    VenteDetail:
      type: object
      required: [id, exportateurId, poidsKg, prixUnitaireFcfa, montantTotalFcfa, dateVente, montantRecuFcfa, soldeDuFcfa, statut, createdAt]
      properties:
        id:
          type: integer
        exportateurId:
          type: integer
        exportateurNom:
          type: ["string", "null"]
        lotId:
          type: ["integer", "null"]
        poidsKg:
          type: string
        prixUnitaireFcfa:
          type: integer
        montantTotalFcfa:
          type: integer
        dateVente:
          type: string
        dateEcheanceReglement:
          type: ["string", "null"]
        montantRecuFcfa:
          type: integer
        soldeDuFcfa:
          type: integer
        statut:
          type: string
          enum: [en_attente, partiel, regle, en_retard]
        createdAt:
          type: string

    VenteInput:
      type: object
      required: [exportateurId, poidsKg, prixUnitaireFcfa, dateVente]
      properties:
        exportateurId:
          type: integer
        lotId:
          type: integer
        poidsKg:
          type: number
        prixUnitaireFcfa:
          type: integer
        dateVente:
          type: string
        dateEcheanceReglement:
          type: string

    EncaissementInput:
      type: object
      required: [montantFcfa]
      properties:
        montantFcfa:
          type: integer

    CreancesSummary:
      type: object
      required: [totalDuFcfa, enRetardFcfa, aEchoirSemaineFcfa, ventes]
      properties:
        totalDuFcfa:
          type: integer
        enRetardFcfa:
          type: integer
        aEchoirSemaineFcfa:
          type: integer
        ventes:
          type: array
          items:
            $ref: "#/components/schemas/VenteDetail"

    # ── M07 Communication ────────────────────────────────────────
    SmsGroupeInput:
      type: object
      required: [message]
      properties:
        message:
          type: string
        groupement:
          type: string

    SmsGroupeResult:
      type: object
      required: [envoyes, echecs, total]
      properties:
        envoyes:
          type: integer
        echecs:
          type: integer
        total:
          type: integer

    SmsHistorique:
      type: object
      required: [id, cooperativeId, message, nbDestinataires, nbEnvoyes, nbEchecs, statut, createdAt]
      properties:
        id:
          type: integer
        cooperativeId:
          type: integer
        message:
          type: string
        groupement:
          type: ["string", "null"]
        nbDestinataires:
          type: integer
        nbEnvoyes:
          type: integer
        nbEchecs:
          type: integer
        statut:
          type: string
          enum: [envoye, echec, partiel]
        createdAt:
          type: string

    # ── M05 Comptabilité ─────────────────────────────────────────
    EcritureComptable:
      type: object
      required: [id, cooperativeId, dateEcriture, libelle, compteDebit, compteCredit, montantFcfa, source, exercice, createdAt]
      properties:
        id:
          type: integer
        cooperativeId:
          type: integer
        dateEcriture:
          type: string
        numeroPiece:
          type: ["string", "null"]
        libelle:
          type: string
        compteDebit:
          type: string
        compteCredit:
          type: string
        montantFcfa:
          type: integer
        source:
          type: string
          enum: [livraison, vente, avance, paiement, manuel]
        sourceId:
          type: ["integer", "null"]
        exercice:
          type: integer
        createdAt:
          type: string

    GrandLivrePage:
      type: object
      required: [ecritures, total, page, limit]
      properties:
        ecritures:
          type: array
          items:
            $ref: "#/components/schemas/EcritureComptable"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer

    BalanceLigne:
      type: object
      required: [numeroCompte, libelle, type, totalDebit, totalCredit, solde]
      properties:
        numeroCompte:
          type: string
        libelle:
          type: string
        type:
          type: string
          enum: [actif, passif, charge, produit]
        totalDebit:
          type: integer
        totalCredit:
          type: integer
        solde:
          type: integer

    MargeCollecte:
      type: object
      required: [caVentesFcfa, coutAchatsFcfa, chargesFcfa, margeNetteFcfa, exercice]
      properties:
        caVentesFcfa:
          type: integer
        coutAchatsFcfa:
          type: integer
        chargesFcfa:
          type: integer
        margeNetteFcfa:
          type: integer
        exercice:
          type: integer
        tauxMarge:
          type: number

    Tresorerie:
      type: object
      required: [soldeBanqueFcfa, soldeCaisseFcfa, totalFcfa]
      properties:
        soldeBanqueFcfa:
          type: integer
        soldeCaisseFcfa:
          type: integer
        totalFcfa:
          type: integer
        dateCalcul:
          type: string

    ConfigComptable:
      type: object
      required: [id, cooperativeId, autoLivraisons, autoPaiements, autoAvances, autoVentesExport, autoEncaissements, autoSalaires, autoStocks]
      properties:
        id:
          type: integer
        cooperativeId:
          type: integer
        autoLivraisons:
          type: boolean
        autoPaiements:
          type: boolean
        autoAvances:
          type: boolean
        autoVentesExport:
          type: boolean
        autoEncaissements:
          type: boolean
        autoSalaires:
          type: boolean
        autoStocks:
          type: boolean
        modifiePar:
          type: integer
          nullable: true
        updatedAt:
          type: string
          nullable: true

    UpdateConfigComptableInput:
      type: object
      properties:
        autoLivraisons:
          type: boolean
        autoPaiements:
          type: boolean
        autoAvances:
          type: boolean
        autoVentesExport:
          type: boolean
        autoEncaissements:
          type: boolean
        autoSalaires:
          type: boolean
        autoStocks:
          type: boolean

    EcritureEnAttente:
      type: object
      required: [id, cooperativeId, source, libelleProppose, compteDebitPropose, compteCreditPropose, montantFcfa, dateProposee, statut, creeLe]
      properties:
        id:
          type: integer
        cooperativeId:
          type: integer
        source:
          type: string
          enum: [livraison, paiement, avance, vente, encaissement, salaire, stock]
        sourceId:
          type: integer
          nullable: true
        libelleProppose:
          type: string
        compteDebitPropose:
          type: string
        compteCreditPropose:
          type: string
        montantFcfa:
          type: integer
        dateProposee:
          type: string
          format: date
        statut:
          type: string
          enum: [en_attente, validee, rejetee, modifiee]
        commentaireComptable:
          type: string
          nullable: true
        creeLe:
          type: string
        traiteLe:
          type: string
          nullable: true
        traitePar:
          type: integer
          nullable: true

    ValiderEcritureInput:
      type: object
      properties:
        compteDebit:
          type: string
        compteCredit:
          type: string
        montantFcfa:
          type: integer
        libelle:
          type: string
        commentaire:
          type: string

    RejeterEcritureInput:
      type: object
      required: [commentaire]
      properties:
        commentaire:
          type: string

    BilanPoste:
      type: object
      required: [compte, libelle, montantFcfa]
      properties:
        compte:
          type: string
        libelle:
          type: string
        montantFcfa:
          type: integer

    BilanEtat:
      type: object
      required: [actif, passif, totalActifFcfa, totalPassifFcfa, exercice]
      properties:
        actif:
          type: array
          items:
            $ref: "#/components/schemas/BilanPoste"
        passif:
          type: array
          items:
            $ref: "#/components/schemas/BilanPoste"
        totalActifFcfa:
          type: integer
        totalPassifFcfa:
          type: integer
        exercice:
          type: integer

    CompteResultatLigne:
      type: object
      required: [compte, libelle, montantFcfa]
      properties:
        compte:
          type: string
        libelle:
          type: string
        montantFcfa:
          type: integer

    VentilationMensuelle:
      type: object
      required: [mois, produitsFcfa, chargesFcfa, resultatFcfa]
      properties:
        mois:
          type: integer
        produitsFcfa:
          type: integer
        chargesFcfa:
          type: integer
        resultatFcfa:
          type: integer

    CompteResultat:
      type: object
      required: [produits, charges, totalProduitsFcfa, totalChargesFcfa, resultatNetFcfa, exercice, ventilationMensuelle]
      properties:
        produits:
          type: array
          items:
            $ref: "#/components/schemas/CompteResultatLigne"
        charges:
          type: array
          items:
            $ref: "#/components/schemas/CompteResultatLigne"
        totalProduitsFcfa:
          type: integer
        totalChargesFcfa:
          type: integer
        resultatNetFcfa:
          type: integer
        exercice:
          type: integer
        ventilationMensuelle:
          type: array
          items:
            $ref: "#/components/schemas/VentilationMensuelle"

    FluxTresorerie:
      type: object
      required: [fluxOperationnelsFcfa, fluxFinancementFcfa, soldeDebutFcfa, soldeFinalFcfa, exercice]
      properties:
        fluxOperationnelsFcfa:
          type: integer
        fluxFinancementFcfa:
          type: integer
        encaissementsExportateursFcfa:
          type: integer
        paiementsProducteursFcfa:
          type: integer
        avancesOctroyes:
          type: integer
        avancesRembourses:
          type: integer
        soldeDebutFcfa:
          type: integer
        soldeFinalFcfa:
          type: integer
        exercice:
          type: integer

    MargeCampagne:
      type: object
      required: [annee, caVentesFcfa, coutAchatsFcfa, margeNetteFcfa]
      properties:
        annee:
          type: integer
        caVentesFcfa:
          type: integer
        coutAchatsFcfa:
          type: integer
        chargesFcfa:
          type: integer
        margeNetteFcfa:
          type: integer
        tauxMarge:
          type: number

    EcritureManuelleInput:
      type: object
      required: [dateEcriture, libelle, compteDebit, compteCredit, montantFcfa]
      properties:
        dateEcriture:
          type: string
        numeroPiece:
          type: string
        libelle:
          type: string
        compteDebit:
          type: string
        compteCredit:
          type: string
        montantFcfa:
          type: integer

    UtilisateurCompte:
      type: object
      required: [id, nom, prenoms, email, role, actif, createdAt]
      properties:
        id:
          type: integer
        nom:
          type: string
        prenoms:
          type: string
        email:
          type: string
        telephone:
          type: string
          nullable: true
        role:
          type: string
          enum: [pca, directeur, comptable, magasinier, responsable_tracabilite, agent_terrain, auditeur]
        actif:
          type: boolean
        cooperativeId:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time

    CreateUserInput:
      type: object
      required: [nom, prenoms, email, role, motDePasse]
      properties:
        nom:
          type: string
        prenoms:
          type: string
        email:
          type: string
          format: email
        telephone:
          type: string
        role:
          type: string
          enum: [pca, directeur, comptable, magasinier, responsable_tracabilite, agent_terrain, auditeur]
        motDePasse:
          type: string
          minLength: 8

    UpdateUserInput:
      type: object
      properties:
        nom:
          type: string
        prenoms:
          type: string
        email:
          type: string
          format: email
        telephone:
          type: string

    ResetPasswordInput:
      type: object
      required: [nouveauMotDePasse]
      properties:
        nouveauMotDePasse:
          type: string
          minLength: 8

    ToggleActifInput:
      type: object
      required: [actif]
      properties:
        actif:
          type: boolean

    Personnel:
      type: object
      required: [id, cooperativeId, nom, prenoms, poste, typeContrat, dateEmbauche, salaireBaseFcfa, sursalaireFcfa, modePaiement, statut, createdAt, updatedAt]
      properties:
        id:
          type: integer
        cooperativeId:
          type: integer
        nom:
          type: string
        prenoms:
          type: string
        poste:
          type: string
        roleSysteme:
          type: ["string", "null"]
        typeContrat:
          type: string
          enum: [cdi, cdd, journalier, stagiaire]
        dateEmbauche:
          type: string
        dateFinContrat:
          type: ["string", "null"]
        salaireBaseFcfa:
          type: integer
        sursalaireFcfa:
          type: integer
        numeroCnps:
          type: ["string", "null"]
        numeroCni:
          type: ["string", "null"]
        modePaiement:
          type: string
          enum: [orange_money, mtn_momo, virement, especes]
        telephonePaiement:
          type: ["string", "null"]
        ribBanque:
          type: ["string", "null"]
        statut:
          type: string
          enum: [actif, suspendu, sorti]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreatePersonnelInput:
      type: object
      required: [nom, prenoms, poste, dateEmbauche, salaireBaseFcfa]
      properties:
        nom:
          type: string
        prenoms:
          type: string
        poste:
          type: string
        roleSysteme:
          type: string
        typeContrat:
          type: string
          enum: [cdi, cdd, journalier, stagiaire]
        dateEmbauche:
          type: string
        dateFinContrat:
          type: string
        salaireBaseFcfa:
          type: integer
        sursalaireFcfa:
          type: integer
        numeroCnps:
          type: string
        numeroCni:
          type: string
        modePaiement:
          type: string
          enum: [orange_money, mtn_momo, virement, especes]
        telephonePaiement:
          type: string
        ribBanque:
          type: string

    UpdatePersonnelInput:
      type: object
      properties:
        nom:
          type: string
        prenoms:
          type: string
        poste:
          type: string
        roleSysteme:
          type: string
        typeContrat:
          type: string
          enum: [cdi, cdd, journalier, stagiaire]
        dateEmbauche:
          type: string
        dateFinContrat:
          type: string
        salaireBaseFcfa:
          type: integer
        sursalaireFcfa:
          type: integer
        numeroCnps:
          type: string
        numeroCni:
          type: string
        modePaiement:
          type: string
          enum: [orange_money, mtn_momo, virement, especes]
        telephonePaiement:
          type: string
        ribBanque:
          type: string
        statut:
          type: string
          enum: [actif, suspendu, sorti]

    PersonnelHistorique:
      type: object
      required: [personnel, bulletins, avances]
      properties:
        personnel:
          $ref: "#/components/schemas/Personnel"
        bulletins:
          type: array
          items:
            $ref: "#/components/schemas/BulletinPaie"
        avances:
          type: array
          items:
            $ref: "#/components/schemas/AvancePersonnel"

    ComposanteSalaire:
      type: object
      required: [id, cooperativeId, libelle, type, calcul, valeur, obligatoire]
      properties:
        id:
          type: integer
        cooperativeId:
          type: integer
        libelle:
          type: string
        type:
          type: string
          enum: [avantage, retenue]
        calcul:
          type: string
          enum: [fixe, pourcentage]
        valeur:
          type: integer
        obligatoire:
          type: boolean

    BulletinPaie:
      type: object
      required: [id, personnelId, cooperativeId, mois, annee, periode, salaireBaseFcfa, totalAvantagesFcfa, totalRetenuesFcfa, salaireBrutFcfa, salaireNetFcfa, coutTotalEmployeurFcfa, statut, createdAt]
      properties:
        id:
          type: integer
        personnelId:
          type: integer
        cooperativeId:
          type: integer
        mois:
          type: integer
        annee:
          type: integer
        periode:
          type: string
        salaireBaseFcfa:
          type: integer
        totalAvantagesFcfa:
          type: integer
        totalRetenuesFcfa:
          type: integer
        salaireBrutFcfa:
          type: integer
        salaireNetFcfa:
          type: integer
        chargesCnpsPatronaleFcfa:
          type: integer
        chargesTaxeApprentissageFcfa:
          type: integer
        chargesFpcFcfa:
          type: integer
        coutTotalEmployeurFcfa:
          type: integer
        statut:
          type: string
          enum: [brouillon, valide, paye]
        dateValidation:
          type: ["string", "null"]
          format: date-time
        datePaiement:
          type: ["string", "null"]
          format: date-time
        referencePaiement:
          type: ["string", "null"]
        payePar:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time

    LigneBulletin:
      type: object
      required: [id, bulletinId, libelle, type, montantFcfa]
      properties:
        id:
          type: integer
        bulletinId:
          type: integer
        libelle:
          type: string
        type:
          type: string
          enum: [avantage, retenue]
        montantFcfa:
          type: integer

    BulletinAvecPersonnel:
      type: object
      required: [bulletin, personnel]
      properties:
        bulletin:
          $ref: "#/components/schemas/BulletinPaie"
        personnel:
          type: object
          required: [id, nom, prenoms, poste]
          properties:
            id:
              type: integer
            nom:
              type: string
            prenoms:
              type: string
            poste:
              type: string
            modePaiement:
              type: string
            telephonePaiement:
              type: ["string", "null"]

    BulletinDetail:
      type: object
      required: [bulletin, personnel, lignes]
      properties:
        bulletin:
          $ref: "#/components/schemas/BulletinPaie"
        personnel:
          $ref: "#/components/schemas/Personnel"
        lignes:
          type: array
          items:
            $ref: "#/components/schemas/LigneBulletin"

    GenererBulletinsInput:
      type: object
      required: [mois, annee]
      properties:
        mois:
          type: integer
        annee:
          type: integer
        personnelIds:
          type: array
          items:
            type: integer

    PayerBulletinInput:
      type: object
      properties:
        referencePaiement:
          type: string

    GenerationResult:
      type: object
      required: [personnelId, bulletinId]
      properties:
        personnelId:
          type: integer
        bulletinId:
          type: integer
        erreur:
          type: ["string", "null"]

    AvancePersonnel:
      type: object
      required: [id, personnelId, cooperativeId, montantFcfa, dateOctroi, statut, montantRembourse, createdAt]
      properties:
        id:
          type: integer
        personnelId:
          type: integer
        cooperativeId:
          type: integer
        montantFcfa:
          type: integer
        dateOctroi:
          type: string
        motif:
          type: ["string", "null"]
        statut:
          type: string
          enum: [en_cours, rembourse]
        montantRembourse:
          type: integer
        createdAt:
          type: string
          format: date-time

    AvanceAvecPersonnel:
      type: object
      required: [avance, personnel]
      properties:
        avance:
          $ref: "#/components/schemas/AvancePersonnel"
        personnel:
          type: object
          required: [id, nom, prenoms, poste]
          properties:
            id:
              type: integer
            nom:
              type: string
            prenoms:
              type: string
            poste:
              type: string

    CreateAvancePersonnelInput:
      type: object
      required: [personnelId, montantFcfa, dateOctroi]
      properties:
        personnelId:
          type: integer
        montantFcfa:
          type: integer
        dateOctroi:
          type: string
        motif:
          type: string

    RapportMensuel:
      type: object
      required: [mois, annee, nbPersonnelActifs, nbBulletins, nbPayes, nbValides, nbBrouillons, totalBrut, totalNet, totalChargesPatronales, coutTotalEmployeur, detailsParPoste]
      properties:
        mois:
          type: integer
        annee:
          type: integer
        nbPersonnelActifs:
          type: integer
        nbBulletins:
          type: integer
        nbPayes:
          type: integer
        nbValides:
          type: integer
        nbBrouillons:
          type: integer
        totalBrut:
          type: integer
        totalNet:
          type: integer
        totalChargesPatronales:
          type: integer
        coutTotalEmployeur:
          type: integer
        detailsParPoste:
          type: array
          items:
            type: object
            required: [poste, nbPersonnel, totalNet]
            properties:
              poste:
                type: string
              nbPersonnel:
                type: integer
              totalNet:
                type: integer

    HistoriqueMasse:
      type: object
      required: [mois, annee, periode, totalBrut, totalNet, coutTotalEmployeur, nbBulletins]
      properties:
        mois:
          type: integer
        annee:
          type: integer
        periode:
          type: string
        totalBrut:
          type: integer
        totalNet:
          type: integer
        coutTotalEmployeur:
          type: integer
        nbBulletins:
          type: integer

    RemboursementAvancePersonnelInput:
      type: object
      properties:
        montantRembourse:
          type: integer

    Campagne:
      type: object
      required: [id, cooperativeId, libelle, anneeDebut, anneeFin, dateOuverture, statut, createdAt]
      properties:
        id:
          type: integer
        cooperativeId:
          type: integer
        libelle:
          type: string
        anneeDebut:
          type: integer
        anneeFin:
          type: integer
        dateOuverture:
          type: string
          format: date
        dateFermeture:
          type: ["string", "null"]
          format: date
        statut:
          type: string
          enum: [ouverte, fermee]
        createdAt:
          type: string
          format: date-time

    FermerCampagneInput:
      type: object
      properties:
        dateFermeture:
          type: string
          format: date

    CampagneInput:
      type: object
      required: [libelle, anneeDebut, anneeFin, dateOuverture]
      properties:
        libelle:
          type: string
        anneeDebut:
          type: integer
        anneeFin:
          type: integer
        dateOuverture:
          type: string
          format: date

    ConfigPartsSociales:
      type: object
      required: [cooperativeId, valeurNominaleFcfa, nbrePartsMin]
      properties:
        cooperativeId:
          type: integer
        valeurNominaleFcfa:
          type: integer
        nbrePartsMin:
          type: integer

    RapportParts:
      type: object
      properties:
        totalMembresSouscripteurs:
          type: ["integer", "null"]
        totalPartsSouscrites:
          type: ["integer", "null"]
        totalSouscritFcfa:
          type: ["integer", "null"]
        totalLibereFcfa:
          type: ["integer", "null"]
        totalResteALibererFcfa:
          type: ["integer", "null"]

    LiberationParts:
      type: object
      required: [id, membreId, cooperativeId, dateVersement, montantFcfa, createdAt]
      properties:
        id:
          type: integer
        membreId:
          type: integer
        cooperativeId:
          type: integer
        dateVersement:
          type: string
          format: date
        codeLiberation:
          type: ["string", "null"]
        versement:
          type: ["string", "null"]
        montantFcfa:
          type: integer
        agentId:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time

    PartsMembre:
      type: object
      required: [membre, liberations, config]
      properties:
        membre:
          type: object
          required: [id, nom, prenoms, nbrePartsSouscrites, valeurNominalePartFcfa, totalSouscritFcfa, totalLibereFcfa, resteALibererFcfa]
          properties:
            id:
              type: integer
            nom:
              type: string
            prenoms:
              type: string
            nbrePartsSouscrites:
              type: integer
            valeurNominalePartFcfa:
              type: integer
            totalSouscritFcfa:
              type: integer
            totalLibereFcfa:
              type: integer
            resteALibererFcfa:
              type: integer
        liberations:
          type: array
          items:
            $ref: "#/components/schemas/LiberationParts"
        config:
          $ref: "#/components/schemas/ConfigPartsSociales"

    LiberationInput:
      type: object
      required: [membreId, montantFcfa, dateVersement]
      properties:
        membreId:
          type: integer
        montantFcfa:
          type: integer
        dateVersement:
          type: string
          format: date
        codeLiberation:
          type: string
        versement:
          type: string

    Refus:
      type: object
      required: [refus]
      properties:
        refus:
          type: object
          required: [id, cooperativeId, venteExportateurId, poidsRefuleKg, motifRefus, statut, createdAt]
          properties:
            id:
              type: integer
            cooperativeId:
              type: integer
            venteExportateurId:
              type: integer
            poidsRefuleKg:
              type: string
            motifRefus:
              type: ["string", "null"]
            decision:
              type: ["string", "null"]
              enum: [retour_stock, declassement, autre_acheteur, perte, null]
            statut:
              type: string
              enum: [en_attente, traite]
            traitePar:
              type: ["integer", "null"]
            traiteLe:
              type: ["string", "null"]
              format: date-time
            createdAt:
              type: string
              format: date-time
        vente:
          type: ["object", "null"]
          properties:
            exportateurId:
              type: integer
            dateVente:
              type: string
            produit:
              type: ["string", "null"]
            nombreSacsRefoules:
              type: ["integer", "null"]
            numeroBonSortie:
              type: ["string", "null"]

    RefusStats:
      type: object
      properties:
        totalRefulesKg:
          type: string
        totalTraitesKg:
          type: string
        totalEnAttenteKg:
          type: string
        nbEnAttente:
          type: integer
        nbTraites:
          type: integer

    TraiterRefusInput:
      type: object
      required: [decision]
      properties:
        decision:
          type: string
          enum: [retour_stock, declassement, autre_acheteur, perte]
        entrepotRetourId:
          type: integer
        ancienGrade:
          type: string
        nouveauGrade:
          type: string
        nouvelExportateurId:
          type: integer
        prixUnitaireNouveauFcfa:
          type: integer
        motifPerte:
          type: string
        pvConstat:
          type: boolean
        tauxHumidite:
          type: number

    Fournisseur:
      type: object
      required: [id, cooperativeId, typeFournisseur, code, nom, actif, createdAt]
      properties:
        id:
          type: integer
        cooperativeId:
          type: integer
        typeFournisseur:
          type: string
          enum: [membre, pisteur, externe]
        membreId:
          type: ["integer", "null"]
        code:
          type: string
        nom:
          type: string
        prenoms:
          type: ["string", "null"]
        sexe:
          type: ["string", "null"]
        telephone:
          type: ["string", "null"]
        section:
          type: ["string", "null"]
        nationalite:
          type: ["string", "null"]
        numeroCni:
          type: ["string", "null"]
        origine:
          type: ["string", "null"]
        dateAdhesion:
          type: ["string", "null"]
          format: date
        lieuNaissance:
          type: ["string", "null"]
        actif:
          type: boolean
        createdAt:
          type: string
          format: date-time

    FournisseurInput:
      type: object
      required: [typeFournisseur, nom]
      properties:
        typeFournisseur:
          type: string
          enum: [pisteur, externe]
        nom:
          type: string
        prenoms:
          type: string
        sexe:
          type: string
        telephone:
          type: string
        section:
          type: string
        nationalite:
          type: string
        numeroCni:
          type: string
        origine:
          type: string
        dateAdhesion:
          type: string
          format: date
        lieuNaissance:
          type: string
