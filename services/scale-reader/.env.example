# Port série de la balance (COM1 sur Windows, /dev/ttyS0 sur Linux)
SCALE_PORT=COM1

# Port WebSocket exposé localement vers le navigateur du peseur
WS_PORT=4001

# Port HTTP de fallback (GET /api/scale/current-weight)
HTTP_PORT=4002

# Niveau de log : debug | info | warn | error
LOG_LEVEL=info
